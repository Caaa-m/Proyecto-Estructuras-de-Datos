#pragma once
#include <vector>
#include <string>

// Ordena alfabeticamente el vector usando QuickSort desde cero.
// Llamar con: quickSort(vec, 0, vec.size() - 1)
void quickSort(std::vector<std::string>& arr, int low, int high);
